# شروع کن از اینجا 🚀

این پوشه، خودِ پوشه پروژه است. آنزیپ که کردی، همه‌چیز سر جای خودش است — لازم نیست فایلی جابه‌جا کنی.

## پیش‌نیازهای سیستم (یک‌بار)
- Docker (به‌همراه Compose) نصب و روشن
- Node.js LTS + npm
- Git
- Claude Code نصب و لاگین‌شده — راهنما: https://docs.claude.com/en/docs/claude-code/overview

## اجرای پروژه با Claude Code
1. ترمینال را داخل همین پوشه باز کن:
   `cd hamyar`
2. Claude Code را اجرا کن:
   `claude`
3. کل متن فایل **PROMPT.md** را کپی و در اولین پیام پیست کن. خودش فاز صفر را شروع می‌کند:
   لاراول را کنار همین فایل‌ها نصب می‌کند، Docker و CI را می‌سازد، دیزاین‌سیستم و
   ابزارها (Laravel Boost و Playwright MCP) را راه می‌اندازد و `docs/ROADMAP.md` را
   با چک‌باکس همه فازها می‌سازد.
4. از جلسه دوم به بعد فقط بنویس:
   `Continue from docs/ROADMAP.md`

## چهار جایی که از تو تأیید می‌خواهد (Decision Gate)
1) طرح ساب‌دامین و لیست نقش‌ها (پایان فاز ۱)
2) جدول قیمت پلن‌ها و فرمول Prorate (فاز ۲)
3) قالب چاپ فاکتور و قواعد رُند کردن (فاز ۵)
4) انتخاب شرکت واسط مودیان + دامنه ویترین (فاز ۱۰) — و بازبینی طراحی لندینگ در فاز ۱۱

## نقشه فایل‌های این پوشه
| فایل/پوشه | چیست |
|---|---|
| `PROMPT.md` | مگاپرامپت شروع — فقط یک‌بار در جلسه اول پیست می‌شود |
| `CLAUDE.md` | قانون اساسی پروژه؛ Claude Code هر جلسه خودکار می‌خواندش |
| `.claude/skills/hamyar-ui/` | اسکیل دیزاین‌سیستم (خودکار لود می‌شود) |
| `docs/01-master-plan-fa.md` | سند جامع فارسی: امکانات، معماری، دیتابیس، نقشه راه |
| `docs/03-design-and-claude-setup-fa.md` | بریف طراحی لندینگ، دیزاین‌سیستم، ابزارها |
| `docs/` (بقیه) | ROADMAP و PROGRESS و ADRها را خودِ Claude Code در فاز ۰ می‌سازد |

## نکته‌ها
- هیچ فایلی از این‌ها را دستی پاک/جابه‌جا نکن؛ پرامپت طوری نوشته شده که لاراول را
  «دور» همین فایل‌ها نصب کند و همه را حفظ کند.
- اگر وسط کار قطع شد یا کانتکست پر شد، نگران نباش: `/clear` بزن و دوباره
  `Continue from docs/ROADMAP.md` — همه پیشرفت در فایل‌هاست، نه در حافظه جلسه.
- هر فاز فقط وقتی بسته می‌شود که `composer test` سبز باشد؛ اگر Claude خواست تستی را
  دور بزند، قبول نکن.
