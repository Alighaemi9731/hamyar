# Strings that broke docs/brand/voice.md when bin/check-copy-terms landed (2026-09-04),
# as `path:term` pairs. The gate fails on anything NOT listed here, and also fails when a
# listed pair disappears — so this file shrinks and cannot rot.
#
# It landed with ten pairs, every one the old landing's colloquial voice («مغازه»,
# «توی کشو», «رُک بگوییم»). All ten came off within the day, section by section, as 16.3
# rebuilt the pages that carried them. **The list is empty, which is the point of a
# ratchet.** Do not add to it: a new string that breaks the voice is a string to rewrite.
